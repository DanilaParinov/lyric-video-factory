# lyric-video-factory
FFmpeg-powered CLI tool that generates multiple lyric videos from a single audio file, lyrics, timed template, and a pool of background videos — each render uses a randomized background selection.
